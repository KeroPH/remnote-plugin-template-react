# AutoCloze plugin (local dev)

This is a local development copy of the RemNote plugin template with the AutoCloze snippet integrated.

See `src/widgets/selected_cloze.tsx` for the selected-text widget that calls OpenAI.

Run `npm install` and `npm run dev` then use RemNote "Develop from localhost" at http://localhost:8080 to test.
